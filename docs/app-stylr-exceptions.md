# App Stylr exceptions

Spottr currently has no intentional visual exceptions from App Stylr v1.0.0.

The in-page capture surface is compact because it must sit above an arbitrary storefront or application. It still uses the dark App Stylr semantic layer, bundled typography guidance, restrained borders, and dialog/control recipes. The host page remains the visual subject of the screenshot; the annotation controls remain isolated in Shadow DOM.
